{
  "name": "metroid-prime-randomizer-website-client",
  "version": "1.0.0",
  "scripts": {
    "ng:serve": "ng serve --proxy-config proxy.conf.json",
    "ng:build:prod": "ng build --prod"
  },
  "private": true
}
